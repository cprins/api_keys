<h1 align= 'center'>Resultado</h1>

<h3 align='center'>Codigo: <%= @Find_duplicated.code %> </h3>
<h3 align='center'> Estatus: <%= @Find_duplicated.status %> </h3>

<% if @Find_duplicated.code == '200' %>
  <h3 align='center'> Palabras repetidas </h3>
  <div class="container">
    <table  class="table table-striped table table-bordered">
      <thead>
        <tr>
          <th>Palabra</th>
          <th>Posiciones en donde se encuentra</th>
        </tr>
      </thead>
      <tbody>
        <% @Find_duplicated.wordposition.each do |k, v| %>
          <tr>
            <td > <%= k %></td>
            <td ><%= v.join(",") %></td>
          </tr>
        <%end %>
      </tbody>
    </table>
  <div class="container">
<% end %>

